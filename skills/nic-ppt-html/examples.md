# Examples — nic-ppt-html

Bundled demo (NicSkills architecture, `project-brand` + `tech-dark`):

[`examples/demo-deck.html`](examples/demo-deck.html)

Open with [`nic-html-preview`](../nic-html-preview/).

## Architecture deck from repo

User: `用 nic-ppt-html 把本仓库做成架构宣讲 PPT HTML，跟项目配色`

1. Scope = workspace  
2. Brand from CSS / docs (`#3d9cf0` family)  
3. `deck-type=architecture`, `style=project-brand` + `tech-dark` skeleton  
4. Write `nic-skills-artifacts/ppt-html/YYYYMMDD-<slug>-deck.html`  
5. Preview with `nic-html-preview` if useful  

## Consulting brief for a folder

User: `把 docs/ 和 README 总结成管理层汇报幻灯片，consulting 风格`

1. Scope = cited paths  
2. `exec-brief` + `consulting`  
3. Action titles; light background  

## Pitch tone

User: `对外 pitch HTML，midnight，突出技能矩阵`

1. `pitch` + `midnight`  
2. KPI / capability grid; keep facts real  

## Hand-offs

| User intent | Skill |
|-------------|--------|
| 单页测试报告，不要翻页 | `nic-visual-report` |
| 只要一个 SVG 图标 | `nic-visual-code` |
| 打开刚才的 deck | `nic-html-preview` |
